#include <stdio.h>

int main() {

    int map[2], p, moves[2], PA[2] = {1, 1};

    for(int i = 0; i < 2; i++) {
        scanf("%d", &map[i]);
    }

    int PB[2] = {map[0], map[1]};

    scanf("%d", &p);

    for(int i = 0; i < p; i++) {}

    return 0;
}